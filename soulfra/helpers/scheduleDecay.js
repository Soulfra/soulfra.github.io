// scheduleDecay.js
// Helper to trigger scheduled decay tasks

function scheduleDecay() {
    console.log('Triggering decay check for all soulprints...');
    // Future: call /api/decay endpoint or run local sweep
}

export default scheduleDecay;
