FROM nginx:alpine

# Copy static files
COPY static /usr/share/nginx/html

# Custom nginx config for single page
RUN echo 'server { listen 3000; location / { root /usr/share/nginx/html; try_files /portal.html =404; } }' > /etc/nginx/conf.d/default.conf

EXPOSE 3000