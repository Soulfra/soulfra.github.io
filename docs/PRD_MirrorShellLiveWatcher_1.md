
# 🪞 PRD: MirrorShellLiveWatcher.js

## Purpose:
Watches key frontend files and pings the backend if hot reload or rebuild is needed (during dev or local usage). Keeps frontend + loop state in sync.

---

## Watches:
- `/mirror-shell/components/`
- `/loop/`
- `/agents/`
- `/stream.txt`

## Optional:
- Visual reload indicator in `/mirror-shell/debug.html`
- Button: “Force Refresh”
