# DeathToData Environment Configuration

# Environment (development or production)
NODE_ENV=development

# API Port
PORT=5051

# PostgreSQL Database (for production)
# Get this from Railway, Render, or Supabase
DATABASE_URL=postgresql://username:password@hostname:5432/deathtodata

# For Railway.app:
# DATABASE_URL will be automatically set when you add PostgreSQL service

# For local SQLite (no config needed):
# SQLite database will be created at ./deathtodata.db automatically
